{
  "name": "@oneworld/contracts",
  "version": "0.1.0",
  "private": true,
  "description": "Shared TypeScript contracts: branded IDs, state machines, domain events, and error codes",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "eslint . --max-warnings 0",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "clean": "rimraf dist .turbo"
  },
  "dependencies": {
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@oneworld/testing": "workspace:*",
    "typescript": "^5.7.3",
    "vitest": "^2.1.8"
  }
}
